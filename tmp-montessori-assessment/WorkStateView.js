function WorkStateView(student) {
  console.log('rendering work-workstate table')
  new TextWidget('work datatable stub'); // work, presented, practiced, mastered (dates, comments)

  // table of work, workstates for each
  // map subject/worklist with list of work ???
}